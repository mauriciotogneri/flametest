name: flametest
description: A Flame test project
publish_to: none
version: 0.0.1+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  get_it: ^7.2.0
  dafluta: ^4.4.0
  
  flame: 1.4.0

  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

dev_dependencies:
  daassets: ^1.3.2
  daunused: ^1.3.0
  dapublock: ^1.0.1
  build_runner: ^2.3.2
  flutter_lints: ^2.0.1
  
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  #assets:
  #  - assets/anim/
  #  - assets/fonts/
  #  - assets/images/logo/